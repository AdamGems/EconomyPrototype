using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class CurrencyManager : MonoBehaviour
{
    public Dictionary<Currency.ID, Currency.Definition> CurrencyDefinitions;

    public void Initialize()
    {
        CurrencyDefinitions = new Dictionary<Currency.ID, Currency.Definition>();
        foreach (Currency.Definition definition in GetComponentsInChildren<Currency.Definition>())
        {
            CurrencyDefinitions.Add(definition.Type, definition);
        }
    }
}
*/
